version: '3.8'

services:
  react-app:
    image: node:20
    container_name: react-app
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    command: sh -c "npm run dev"